# Détection automatique

Architecture : **Monolithic**

Confiance : 50%

## Classement

| Architecture | Score | Confiance |
|---|---|---|
| Monolithic | 0 | 50% |

## Analyse IA

Fichier source. Nombre de lignes: 0.

Fichier source. Nombre de lignes: 232.

Le fichier `Main` est un programme Java qui a pour objet principal d'initialiser et d'utiliser une classe de calculatrice (`Calculatrice`) dans le but de réaliser des opérations mathématiques.

Les classes/fonctions importantes sont :

* La classe `Main`, qui est la classe d'entrée du programme et qui instancie l'objet `Calculatrice`.
* La classe `Calculatrice`, qui contient les méthodes pour effectuer des opérations mathématiques (par exemple, addition, soustraction, multiplication, division, etc.).

Les dépendances sont :

* La bibliothèque Java standard (`java.lang`) est utilisée pour accéder aux types de base tels que `String` et `Object`.
* La classe `Calculatrice` est supposée être définie dans un fichier séparé, qui n'est pas fourni dans le code.