# Analyse Architecture

Architecture détectée : **Flask Application**

Confiance : 100%

## Signaux

- Import Flask détecté dans le code (`import flask` / `from flask import ...`)
- 62 route(s) Flask/Blueprint détectée(s) (@app.route, @blueprint.route...)
- App factory pattern détecté (fonction `create_app()`)
- Dossier templates/ détecté
- Dossier static/ détecté
- Manifestes Python détectés